export default (state) => {
  return {
    customers: state.customers,
    tags: state.tags,
    tree: state.tree,
    houses: state.houses,
    tracks: state.tracks,
    codewordTypes: state.codewords.codewordTypes,
  }
}
